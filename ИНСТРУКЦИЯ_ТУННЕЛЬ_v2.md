# 🌐 ТУННЕЛЬ И СЕТЕВОЙ ДОСТУП v2.0

## 📋 ПРОБЛЕМЫ (БЫЛО)

1. ❌ При запуске приходила "чушь" с IP 127.0.0.1
2. ❌ Пароль туннеля = локальный IP (бессмыслица)
3. ❌ Ссылка на туннель не работала
4. ❌ Не было логирования подключений
5. ❌ Непонятно, кто и когда подключается

---

## ✅ РЕШЕНИЯ (СТАЛО)

### 1. **Правильные IP-адреса**
- **Локальный IP** (Wi-Fi сеть): `http://192.168.1.207:8080`
- **Внешний IP** (интернет): `http://81.23.181.238:8080`
- **Туннель** (loca.lt): `https://random-name.loca.lt`

### 2. **Актуальные уведомления**
Администратор получает сообщение с:
- ✅ Рабочей ссылкой на туннель
- ✅ Паролем (внешний IP)
- ✅ Локальной ссылкой для Wi-Fi
- ✅ Внешней ссылкой для интернета

### 3. **Логирование подключений**
Все подключения записываются в:
```
C:\Users\User\Desktop\GrafikRaboty\logs\tunnel_connections.log
```

Формат записи:
```
[2026-03-24 15:30:45] connected - https://rotten-hands-run.loca.lt
[2026-03-24 15:35:12] health_check_ok - https://rotten-hands-run.loca.lt
```

### 4. **Мониторинг здоровья туннеля**
- Проверка каждые 30 секунд
- Автоматический рестарт при проблемах
- Уведомление после MAX_RESTARTS (5 попыток)

---

## 🚀 ИСПОЛЬЗОВАНИЕ

### Вариант 1: Новый Tunnel Manager v2 (РЕКОМЕНДУЕТСЯ)

```bash
# Запуск через Python
python tunnel_manager_v2.py
```

**Преимущества:**
- ✅ Полное логирование
- ✅ Проверка здоровья туннеля
- ✅ Автоматический рестарт
- ✅ Правильные IP в уведомлениях

### Вариант 2: Старый tunnel_monitor.py

```bash
# Запуск через Python
python tunnel_monitor.py
```

---

## 📱 ССЫЛКИ ДЛЯ СОТРУДНИКОВ

### 1. **В той же сети (Wi-Fi)**
```
http://192.168.1.207:8080
```
**Кому:** Сотрудники дома в той же Wi-Fi сети

### 2. **Из интернета (туннель)**
```
https://rotten-hands-run.loca.lt
Пароль: 81.23.181.238
```
**Кому:** Сотрудники вне дома (работа, магазин)

### 3. **Из интернета (проброс портов)**
```
http://81.23.181.238:8080
```
**Кому:** Если настроен проброс портов на роутере

---

## 🔧 НАСТРОЙКА

### 1. Проверка Node.js
```bash
node --version
```
Должно показать: `v25.6.0` или новее

### 2. Установка localtunnel
```bash
npm install -g localtunnel
```

### 3. Проверка VK конфигурации
Файл: `vk_config.json`
```json
{
  "service_token": "vk1.a.xxx...",
  "admin_vk_id": 146411666,
  "chat_peer_id": 2000000001,
  "group_id": 199112265
}
```

### 4. Запуск сервера
```bash
# Вариант 1: Через батник
ЗАПУСТИТЬ_СЕРВЕР.bat

# Вариант 2: Через Python
python web_server.py
```

### 5. Запуск туннеля
```bash
# Вариант 1: Новый менеджер (рекомендуется)
python tunnel_manager_v2.py

# Вариант 2: Прямой запуск
lt -p 8080
```

---

## 📊 ЛОГИРОВАНИЕ

### Файлы логов

| Файл | Что записывается |
|------|------------------|
| `logs/tunnel_monitor.log` | События мониторинга туннеля |
| `logs/tunnel_connections.log` | Подключения к туннелю |
| `logs/web_server.log` | Запросы к веб-серверу |
| `tunnel_info.json` | Текущая информация о туннеле |

### Просмотр логов
```bash
# Последние 20 строк
tail -20 logs/tunnel_connections.log

# Все логи в реальном времени
Get-Content logs\tunnel_monitor.log -Wait -Tail 50
```

---

## 🛠️ ДИАГНОСТИКА

### 1. Проверка локального сервера
```bash
curl http://127.0.0.1:8080/api/health
```
Должно вернуть: `{"status": "ok"}`

### 2. Проверка туннеля
```bash
curl https://your-tunnel.loca.lt/api/health
```

### 3. Проверка порта
```bash
netstat -an | findstr :8080
```
Должно быть: `0.0.0.0:8080` или `192.168.x.x:8080`

### 4. Проверка внешнего IP
```bash
curl https://api.ipify.org
```

### 5. Тест VK уведомлений
```bash
python test_vk_tunnel_notify.py
```

---

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### 1. "Туннель не работает"
**Причина:** Loca.lt сменил URL или упал
**Решение:** Перезапустить туннель
```bash
# Остановить текущий (Ctrl+C)
# Запустить заново
python tunnel_manager_v2.py
```

### 2. "Сотрудники не могут подключиться"
**Причина:** Туннель недоступен
**Решение:** Дать локальную ссылку
```
http://192.168.1.207:8080
```

### 3. "Приходит IP 127.0.0.1"
**Причина:** Не определился внешний IP
**Решение:** Проверить интернет
```bash
ping 8.8.8.8
```

### 4. "Пароль не подходит"
**Причина:** Пароль = внешний IP, а не 127.0.0.1
**Решение:** Использовать актуальный пароль из уведомления

---

## 📋 ЧЕКЛИСТ ЗАПУСКА

- [ ] Node.js установлен (`node --version`)
- [ ] localtunnel установлен (`lt --version`)
- [ ] VK токен настроен (`vk_config.json`)
- [ ] Сервер запущен (`ЗАПУСТИТЬ_СЕРВЕР.bat`)
- [ ] Туннель запущен (`python tunnel_manager_v2.py`)
- [ ] Уведомление пришло в VK
- [ ] Ссылка из уведомления открывается
- [ ] Сотрудники могут подключиться

---

## 🎯 ИТОГ

**Теперь у вас:**
- ✅ Правильные IP-адреса в уведомлениях
- ✅ Рабочие ссылки для сотрудников
- ✅ Логирование всех подключений
- ✅ Мониторинг здоровья туннеля
- ✅ Автоматический рестарт при проблемах

**Ссылки для сотрудников:**
1. **Wi-Fi:** `http://192.168.1.207:8080`
2. **Туннель:** `https://xxx.loca.lt` (из уведомления)
3. **Интернет:** `http://81.23.181.238:8080`

**Пароль:** Ваш внешний IP (например, `81.23.181.238`)

---

*Версия: 2.0*
*Дата: 24.03.2026*
