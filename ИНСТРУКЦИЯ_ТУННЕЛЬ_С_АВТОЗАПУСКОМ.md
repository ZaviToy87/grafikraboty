# 🚀 ТУННЕЛЬ С АВТОМАТИЧЕСКИМ ПЕРЕЗАПУСКОМ

## 📋 Обзор

Система автоматического мониторинга туннеля с:
- ✅ **Автоматическим перезапуском** при падении
- ✅ **VK уведомлениями** о каждом перезапуске
- ✅ **Неограниченным количеством попыток** (или лимит)
- ✅ **Логированием** всех событий

---

## 🎯 Возможности

### 1. Автоматический перезапуск
- Монитор проверяет туннель каждые 30 секунд
- При обнаружении проблемы - автоматический перезапуск
- Задержка между перезапусками: 5 секунд

### 2. VK уведомления
- **При запуске**: новая ссылка туннеля + пароль
- **При перезапуске**: номер попытки + новая ссылка
- **При ошибке**: сообщение о проблеме после достижения лимита

### 3. Получатели уведомлений
- **Администратор** (admin_vk_id: 146411666)
- **Чат сообщества** (chat_peer_id: 2000000001)

---

## 🚀 Запуск

### Вариант 1: Сервер с туннелем (рекомендуется)
```batch
ЗАПУСТИТЬ_СЕРВЕР_С_ТУННЕЛЕМ.bat
```

**Что происходит:**
1. Запускается веб-сервер на порту 8080
2. Запускается монитор туннеля
3. Туннель создаётся и отправляется в VK
4. При падении - автоматический перезапуск + VK уведомление

### Вариант 2: Только сервер (без монитора)
```batch
ЗАПУСТИТЬ_СЕРВЕР.bat
```

### Вариант 3: Только монитор (если сервер уже запущен)
```bash
python tunnel_monitor.py --port 8080 --max-restarts -1 --restart-delay 5
```

---

## ⚙️ Настройка

### Параметры командной строки

```bash
python tunnel_monitor.py [OPTIONS]

Опции:
  --port PORT           Локальный порт (по умолчанию: 8080)
  --max-restarts N      Максимум перезапусков (-1 = безлимитно)
  --restart-delay N     Задержка между перезапусками (секунды)
```

### Примеры

**Безлимитные перезапуски:**
```bash
python tunnel_monitor.py --port 8080 --max-restarts -1
```

**Максимум 5 перезапусков:**
```bash
python tunnel_monitor.py --port 8080 --max-restarts 5 --restart-delay 10
```

---

## 📁 Файлы

| Файл | Назначение |
|------|------------|
| `tunnel_monitor.py` | Главный файл монитора |
| `ЗАПУСТИТЬ_СЕРВЕР_С_ТУННЕЛЕМ.bat` | Batch для запуска |
| `Остановить_все.bat` | Остановка всего |
| `logs/tunnel_monitor.log` | Логи монитора |
| `logs/tunnel.log` | Логи туннеля |
| `tunnel_info.json` | Текущая информация о туннеле |

---

## 🔔 VK Уведомления

### Формат сообщения при запуске
```
✅ Туннель запущен

🌐 Ссылка:
https://abcd-1234.loca.lt

🔐 Пароль: admin

Время: 24.03.2026 12:00:00

Сервер работает стабильно.
```

### Формат сообщения при перезапуске
```
⚠️ Туннель перезапущен (попытка 3)

🌐 Новая ссылка:
https://efgh-5678.loca.lt

🔐 Пароль: admin

Время: 24.03.2026 12:05:00

Если туннель не работает — проверьте интернет.
```

### Формат сообщения об ошибке
```
❌ Туннель недоступен!

Превышен лимит перезапусков (5).

Ошибка: Tunnel process exited with code 1

Время: 24.03.2026 12:10:00

Требуется ручное вмешательство:
• Проверьте интернет
• Проверьте, запущен ли сервер на порту 8080
• Перезапустите сервер вручную
```

---

## 🛠️ Требования

### Обязательные
- ✅ **Python 3.8+**
- ✅ **Node.js** (для localtunnel)
- ✅ **VK Token** в `vk_config.json`

### Проверка Node.js
```bash
node --version
npx --version
```

### Установка Node.js
Если Node.js не установлен:
1. Скачайте с https://nodejs.org
2. Установите LTS версию
3. Перезапустите терминал

---

## 📊 Мониторинг

### Проверка статуса туннеля

**Через веб-интерфейс:**
```
http://localhost:8080/tunnel-status
```

**Через API:**
```bash
curl http://localhost:8080/api/tunnel-status
```

**Логи монитора:**
```bash
type logs\tunnel_monitor.log
```

**Последние 20 строк:**
```bash
powershell -Command "Get-Content logs\tunnel_monitor.log -Tail 20"
```

---

## 🐛 Решение проблем

### Туннель не запускается

**Проблема:** Node.js не найден
```
⚠️  Node.js not found!
```

**Решение:**
1. Установите Node.js с https://nodejs.org
2. Или укажите путь в `telegram_config.json`:
   ```json
   {"node_dir": "C:\\Program Files\\nodejs"}
   ```

### Туннель постоянно падает

**Проблема:** Нестабильный интернет или localtunnel.me

**Решение:**
1. Проверьте интернет
2. Увеличьте задержку между перезапусками:
   ```bash
   python tunnel_monitor.py --restart-delay 30
   ```
3. Используйте лимит перезапусков:
   ```bash
   python tunnel_monitor.py --max-restarts 10
   ```

### VK уведомления не приходят

**Проблема:** Неверный токен или ID

**Решение:**
1. Проверьте `vk_config.json`:
   ```json
   {
     "token": "ваш_токен",
     "group_id": 199112265,
     "admin_vk_id": 146411666,
     "chat_peer_id": 2000000001
   }
   ```
2. Проверьте логи: `logs\tunnel_monitor.log`

---

## 📝 Примеры использования

### Сценарий 1: Стабильная работа
```
12:00:00 - Туннель запущен: https://abcd-1234.loca.lt
12:30:00 - Туннель работает стабильно
13:00:00 - Туннель работает стабильно
```

### Сценарий 2: Сбой и перезапуск
```
12:00:00 - Туннель запущен: https://abcd-1234.loca.lt
12:15:00 - ⚠️ Туннель не здоров!
12:15:00 - 🔄 Перезапуск #1
12:15:05 - ✅ Туннель перезапущен: https://efgh-5678.loca.lt
12:15:05 - 📤 VK уведомление отправлено
```

### Сценарий 3: Множественные сбои
```
12:00:00 - Туннель запущен
12:10:00 - Перезапуск #1
12:10:30 - Перезапуск #2
12:11:00 - Перезапуск #3
...
12:20:00 - Перезапуск #5 (лимит)
12:20:00 - ❌ VK уведомление об ошибке
12:20:00 - 🛑 Монитор остановлен
```

---

## 🔐 Безопасность

### Пароль туннеля
- По умолчанию используется публичный IP
- Хранится в `tunnel_info.json`
- Отправляется в VK только при запуске/перезапуске

### VK Token
- Хранится в `vk_config.json`
- Требуется только для группы/админа
- Не передаётся третьим лицам

---

## 📞 Поддержка

### Логи для отладки
1. `logs/tunnel_monitor.log` - логи монитора
2. `logs/tunnel.log` - логи туннеля
3. `tunnel_info.json` - текущий статус

### Команды для диагностики
```bash
# Проверка порта 8080
netstat -ano | find ":8080"

# Проверка процессов Python
tasklist | find "python"

# Проверка Node.js
node --version
npx --version

# Просмотр логов
type logs\tunnel_monitor.log
```

---

## 📈 Статистика

Файл `tunnel_info.json` содержит:
```json
{
  "tunnel_url": "https://abcd-1234.loca.lt",
  "password": "admin",
  "started_at": "2026-03-24T12:00:00",
  "restart_count": 3
}
```

---

## ✅ Чек-лист перед запуском

- [ ] Node.js установлен и доступен
- [ ] VK токен действителен
- [ ] Порт 8080 свободен
- [ ] Интернет стабилен
- [ ] `vk_config.json` настроен

---

**Версия:** 1.0  
**Дата:** 24.03.2026  
**Автор:** Сосямба Assistant
