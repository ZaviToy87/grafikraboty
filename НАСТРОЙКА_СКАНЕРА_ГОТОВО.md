# ✅ НАСТРОЙКА СКАНЕРА ЗАВЕРШЕНА

**Дата:** 2 апреля 2026 г.

---

## 🎯 ЧТО СДЕЛАНО

### Исправлена конфигурация COM-портов:

**Файл:** `com_scanner.py`

```python
COM_PORTS = {
    # COM3 ОТКЛЮЧЁН — теперь свободен для 1С ✅
    'scanner_2': {'port': 'COM6', 'baudrate': 9600, 'name': 'Сканер 2 (Основной)'},
}
```

---

## 📊 РАСПРЕДЕЛЕНИЕ ПОРТОВ

| Порт | Статус | Программа |
|------|--------|-----------|
| **COM3** | ✅ СВОБОДЕН | 1С (сканер штрих-кодов) |
| **COM6** | ✅ ЗАНЯТ | Веб-сервер ГрафикРаботы |

---

## 🚀 ЧТОБЫ ЗАПУСТИТЬ

### 1. Перезапусти сервер

Закрой текущий сервер (Ctrl+C) и запусти заново:

```bash
ЗАПУСТИТЬ_СЕРВЕР.bat
```

Или с туннелем:
```bash
ЗАПУСТИТЬ_СЕРВЕР_С_ТУННЕЛЕМ.bat
```

### 2. Проверь логи

В логах должно быть:
```
📡 Запуск COM-сканеров штрих-кодов...
[Сканер 2 (Основной)] Попытка открытия COM6...
[Сканер 2 (Основной)] Порт COM6 открыт успешно!
✅ Все COM-сканеры запущены
```

**НЕ должно быть ошибок про COM3!**

### 3. Проверь работу 1С

1. Запусти 1С
2. Отсканируй штрих-код в 1С
3. ✅ Должно работать!

### 4. Проверь работу веб-сервера

1. Открой `http://localhost:8080`
2. Зайди в "Ревизия товара"
3. Нажми "Добавить товар"
4. Поставь курсор в поле "Штрих-код"
5. Отсканируй товар
6. ✅ Поле должно заполниться автоматически!

---

## 🧪 ТЕСТОВАНИЕ

### Тест 1: Быстрая проверка

Открой в браузере:
```
http://localhost:8080/test-scanner
```

Отсканируй штрих-код — он должен появиться на экране!

### Тест 2: Проверка логов

```powershell
Get-Content logs\web_server.log -Tail 50 | Select-String "Штрих-код"
```

Должно показать:
```
Штрих-код отсканирован (Сканер 2 (Основной)): 4607076138611
```

### Тест 3: Статус сканера

Открой в браузере:
```
http://localhost:8080/api/com-scanner/status
```

Должно вернуться:
```json
{
  "status": "success",
  "scanners": [
    {
      "name": "Сканер 2 (Основной)",
      "port": "COM6",
      "baudrate": 9600
    }
  ]
}
```

---

## 🔧 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### 1С не видит сканер

**Проверь:**
1. COM3 действительно свободен?
   ```bash
   python test_com_scanner_debug.py
   ```
2. В 1С указан правильный COM-порт? (должен быть COM3)
3. Перезапусти 1С после запуска веб-сервера

### Веб-сервер не видит сканер

**Проверь:**
1. COM6 подключен?
2. Сканируешь ли ты прямо в поле ввода?
3. Открой консоль браузера (F12) — там должны быть логи

### Оба не работают одновременно

**Это нормально!** Один сканер не может работать в двух программах сразу.

**Решение:**
- Либо переключи сканер в режим эмуляции клавиатуры (HID)
- Либо используй два разных сканера (один на COM3, другой на COM6)

---

## 📞 ПОДДЕРЖКА

Если проблема осталась:

1. Запусти диагностику:
   ```bash
   python test_com_scanner_debug.py
   ```

2. Сохрани вывод в файл

3. Посмотри логи:
   ```bash
   Get-Content logs\web_server.log -Tail 100
   ```

4. Пришли оба файла

---

## ✅ ЧЕКЛИСТ

- [ ] Сервер перезапущен
- [ ] В логах нет ошибок COM3
- [ ] В логах есть "COM6 открыт успешно"
- [ ] 1С видит сканер (COM3)
- [ ] Веб-сервер видит сканер (COM6)
- [ ] Тестовая страница работает

---

**🎉 ГОТОВО! COM3 свободен для 1С, COM6 работает в веб-сервере!**
