# 🎯 УПРАВЛЕНИЕ СКАНЕРОМ С КНОПКОЙ

**Дата:** 2 апреля 2026 г.  
**Версия:** v4.37

---

## ✅ ЧТО СДЕЛАНО

### Главное изменение:
**Добавлена кнопка включения/выключения сканера** прямо в интерфейсе "Ревизия товара"

Теперь:
- ❌ Сканеры **НЕ** запускаются автоматически при старте сервера
- ✅ Сканеры **включаются кнопкой** когда нужны
- ✅ **COM3 свободен** для 1С всегда
- ✅ **COM6 занимается** только когда сканер включён

---

## 📊 КАК ЭТО РАБОТАЕТ

### 1. Запускаешь сервер
```bash
ЗАПУСТИТЬ_СЕРВЕР.bat
```

В логах увидишь:
```
COM scanners initialized (disabled by default - user can enable in UI)
```

**Сканеры ВЫКЛЮЧены по умолчанию!** ✅

### 2. Открываешь "Ревизия товара"

Вверху формы добавления товара увидишь:

```
┌─────────────────────────────────────────────────────┐
│ 📡 COM-сканер штрих-кодов                           │
│ COM6: сканер в программе | COM3: свободен для 1С    │
│                                      [⚫ Сканер      │
│                                       выключен]     │
└─────────────────────────────────────────────────────┘
```

### 3. Нажимаешь кнопку

Кнопка меняет состояние:

**ВЫКЛ → ВКЛ:**
- ⚫ → 🟢
- "Сканер выключен" → "Сканер включён"
- Кнопка серая → зелёная
- **COM6 занимается**
- Опрос сканера запускается (каждые 300мс)

**ВКЛ → ВЫКЛ:**
- 🟢 → ⚫
- "Сканер включён" → "Сканер выключен"
- Кнопка зелёная → серая
- **COM6 освобождается**
- Опрос сканера останавливается

---

## 🎯 СЦЕНАРИЙ ИСПОЛЬЗОВАНИЯ

### Утро (начало смены):

1. **Запускаешь сервер** → сканеры выключены ✅
2. **Работаешь в 1С** → сканер работает в 1С (COM3 свободен) ✅
3. **Нужно добавить товар в ревизию:**
   - Открываешь "Ревизия товара"
   - Нажимаешь кнопку **"Сканер выключен"** → становится **"Сканер включён"** 🟢
   - Сканируешь штрих-код → поле заполняется автоматически ✅
   - Заполняешь остальные поля → сохраняешь
   - Нажимаешь кнопку **"Сканер включён"** → становится **"Сканер выключен"** ⚫
   - **COM6 освобождается**
4. **Продолжаешь работать в 1С** → сканер снова работает в 1С ✅

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Новые API эндпоинты:

```
POST /api/com-scanner/toggle    — Переключить вкл/выкл
POST /api/com-scanner/enable    — Включить
POST /api/com-scanner/disable   — Выключить
GET  /api/com-scanner/status    — Узнать статус
```

### Пример ответа /api/com-scanner/status:

```json
{
  "status": "success",
  "enabled": true,
  "scanners": [
    {
      "name": "Сканер 2 (Основной)",
      "port": "COM6",
      "baudrate": 9600
    }
  ],
  "last_barcode": {
    "barcode": "4607076138611",
    "timestamp": "2026-04-02T14:30:00"
  }
}
```

### Файлы обновлены:

1. **com_scanner.py** — добавлены функции:
   - `enable_scanners()` — включить
   - `disable_scanners()` — выключить
   - `get_scanners_status()` — статус
   - `scanners_enabled` — глобальный флаг

2. **web_com_scanner.py** — добавлены API:
   - `/api/com-scanner/toggle`
   - `/api/com-scanner/enable`
   - `/api/com-scanner/disable`

3. **dashboard.html** — добавлены:
   - Кнопка управления в форме добавления товара
   - Функции `toggleScanner()`, `updateScannerStatusUI()`, `checkScannerStatus()`
   - Автоматическая проверка статуса при загрузке

4. **web_server.py** — изменено:
   - Сканеры НЕ запускаются автоматически
   - Только инициализация

---

## 📋 ПРЕИМУЩЕСТВА

| Было | Стало |
|------|-------|
| Сканеры включаются при старте | Сканеры выключены по умолчанию |
| COM3 заблокирован сразу | COM3 свободен всегда |
| 1С не работает после запуска | 1С работает всегда ✅ |
| Нельзя выключить | Можно вкл/выкл кнопкой ✅ |
| В админке | Прямо в ревизии товаров ✅ |

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Проверь кнопку

1. Открой "Ревизия товара"
2. Нажми "Добавить товар"
3. Увидишь жёлтый блок с кнопкой
4. Нажми кнопку
5. Должно появиться уведомление: "✅ Сканер включён"
6. Кнопка станет зелёной с текстом "Сканер включён"

### 2. Проверь сканирование

1. Включи сканер кнопкой
2. Поставь курсор в поле "Штрих-код"
3. Отсканируй товар
4. Поле должно заполниться автоматически ✅
5. Поле подсветится зелёным на 1.5 сек

### 3. Проверь выключение

1. Нажми кнопку ещё раз
2. Должно появиться уведомление: "🛑 Сканер выключен"
3. Кнопка станет серой
4. COM6 должен освободиться

### 4. Проверь 1С

1. Выключи сканер в веб-сервере
2. Отсканируй штрих-код в 1С
3. Должно работать! ✅

---

## ⚠️ ВАЖНО

1. **Перед работой в 1С** — выключай сканер в веб-сервере
2. **Перед сканированием в ревизии** — включай сканер кнопкой
3. **После сканирования** — выключай сканер (освободи COM6)

---

## 🎯 ИДЕАЛЬНЫЙ ПОТОК

```
┌─────────────────────┐
│  Запуск сервера     │
│  Сканер ВЫКЛ ⚫     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Работа в 1С        │
│  COM3 свободен ✅   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Нужна ревизия?     │
└──────────┬──────────┘
           │
     ┌─────┴─────┐
     │   ДА      │
     ▼           ▼
┌─────────┐   ┌──────────┐
│ Включи  │   │ НЕТ      │
│ сканер  │   │          │
│ 🟢      │   │          │
└────┬────┘   └──────────┘
     │
     ▼
┌─────────────────────┐
│  Сканируй штрих-код │
│  Поле заполняется   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Выключи сканер ⚫  │
│  COM3 свободен ✅   │
└─────────────────────┘
```

---

## ✅ ЧЕКЛИСТ

- [ ] Сервер запущен
- [ ] В логах "COM scanners initialized (disabled)"
- [ ] Открыта "Ревизия товара"
- [ ] Видна кнопка управления сканером
- [ ] Кнопка серая с текстом "Сканер выключен"
- [ ] При нажатии кнопка становится зелёной
- [ ] При сканировании поле заполняется
- [ ] 1С работает когда сканер выключен

---

**🎉 ГОТОВО! Теперь ты полностью контролируешь сканер!**
