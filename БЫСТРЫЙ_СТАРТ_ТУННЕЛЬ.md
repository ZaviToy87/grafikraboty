# 🚀 БЫСТРЫЙ СТАРТ: ТУННЕЛЬ С АВТОЗАПУСКОМ

## ✅ Что нового

Теперь туннель работает **стабильно** и **автоматически перезапускается** при сбоях!

### Возможности
- 🔄 **Автоматический перезапуск** при падении туннеля
- 🔔 **VK уведомления** о каждом перезапуске с новой ссылкой
- ♾️ **Безлимитные попытки** (или настройте лимит)
- 📊 **Мониторинг** через веб-интерфейс

---

## 🎯 Использование

### 1️⃣ Запуск сервера с туннелем (рекомендуется)

```batch
ЗАПУСТИТЬ_СЕРВЕР_С_ТУННЕЛЕМ.bat
```

**Что происходит:**
- ✅ Запускается веб-сервер
- ✅ Запускается монитор туннеля
- ✅ Туннель создаётся автоматически
- ✅ В VK приходит ссылка на туннель
- ✅ При падении - перезапуск + VK уведомление

### 2️⃣ Остановка всего

```batch
Остановить_сервер.bat
```

Останавливает и сервер, и монитор туннеля.

---

## 📱 VK Уведомления

### При запуске
```
✅ Туннель запущен
🌐 https://abcd-1234.loca.lt
🔐 Пароль: admin
```

### При перезапуске
```
⚠️ Туннель перезапущен (попытка 2)
🌐 https://efgh-5678.loca.lt
🔐 Пароль: admin
```

---

## ⚙️ Настройка (опционально)

### Изменить параметры

Откройте `ЗАПУСТИТЬ_СЕРВЕР_С_ТУННЕЛЕМ.bat` и найдите строку:
```batch
python tunnel_monitor.py --port 8080 --max-restarts -1 --restart-delay 5
```

### Параметры
- `--port 8080` - порт сервера
- `--max-restarts -1` - лимит перезапусков (-1 = безлимитно)
- `--restart-delay 5` - задержка между перезапусками (сек)

### Пример с лимитом
```batch
python tunnel_monitor.py --port 8080 --max-restarts 5 --restart-delay 10
```

---

## 📊 Мониторинг

### Веб-интерфейс
- Статус туннеля: `http://localhost:8080/tunnel-status`
- API статус: `http://localhost:8080/api/tunnel-status`
- API логи: `http://localhost:8080/api/tunnel-logs`

### Логи
- `logs/tunnel_monitor.log` - логи монитора
- `logs/tunnel.log` - логи туннеля
- `tunnel_info.json` - текущая информация

---

## 🐛 Если что-то пошло не так

### Туннель не запускается
1. Проверьте Node.js: `node --version`
2. Если не установлен - скачайте с https://nodejs.org

### VK уведомления не приходят
1. Проверьте `vk_config.json`
2. Убедитесь, что токен действителен
3. Проверьте логи: `logs/tunnel_monitor.log`

### Туннель постоянно падает
1. Проверьте интернет
2. Увеличьте задержку: `--restart-delay 30`
3. Установите лимит: `--max-restarts 5`

---

## 📁 Файлы

| Файл | Назначение |
|------|------------|
| `tunnel_monitor.py` | Монитор туннеля |
| `ЗАПУСТИТЬ_СЕРВЕР_С_ТУННЕЛЕМ.bat` | Запуск сервера с туннелем |
| `Остановить_сервер.bat` | Остановка всего |
| `ТЕСТ_ТУННЕЛЯ.bat` | Тестовый запуск (3 попытки) |
| `ИНСТРУКЦИЯ_ТУННЕЛЬ_С_АВТОЗАПУСКОМ.md` | Полная документация |

---

## ✅ Чек-лист перед запуском

- [ ] Node.js установлен (`node --version`)
- [ ] Порт 8080 свободен
- [ ] VK токен в `vk_config.json`
- [ ] Интернет стабилен

---

**Готово!** Теперь туннель работает стабильно и автоматически перезапускается! 🎉

---

*Версия: 1.0 | Дата: 24.03.2026 | Сосямба Assistant*
