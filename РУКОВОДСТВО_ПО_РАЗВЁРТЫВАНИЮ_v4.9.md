# 📦 GRAFIKRABOTY v4.9 — РУКОВОДСТВО ПО РАЗВЁРТЫВАНИЮ

**Дата:** 23.03.2026  
**Версия:** 4.9  
**Тип:** Полный установочный пакет

---

## 🎯 ОБЗОР

Создано **2 варианта распространения**:

### 1. ZIP Установочный пакет (25.84 MB)
**Файл:** `installer_dist/GrafikRaboty_Installer_v4.9.zip`

**Что внутри:**
- ✅ Все исходные файлы проекта
- ✅ Базы данных (schedule.db)
- ✅ MCP система с агентами (9 штук)
- ✅ Конфигурационные файлы
- ✅ Скрипты установки и запуска
- ✅ requirements.txt со всеми зависимостями

**Для кого:** Для развёртывания на сервере с Python

**Требования:**
- Python 3.11+
- 100 MB свободного места
- Порты 8080 и 8081

---

### 2. EXE Standalone Версия
**Файл:** `exe_dist/GrafikRaboty_Server/`

**Что внутри:**
- ✅ GrafikRaboty_Server.exe (standalone)
- ✅ Все ресурсы (шаблоны, статика, MCP)
- ✅ Базы данных
- ✅ Конфигурации
- ✅ RUN.bat (удобный лаунчер)

**Для кого:** Для запуска без установки Python

**Требования:**
- Windows 10/11
- 500 MB свободного места
- Порты 8080 и 8081

---

## 🚀 ВАРИАНТ 1: УСТАНОВКА ИЗ ZIP

### Шаг 1: Распаковка
```bash
# Распакуйте архив в нужную директорию
C:\GrafikRaboty\
```

### Шаг 2: Установка
```bash
# Перейдите в папку
cd C:\GrafikRaboty

# Запустите установщик
INSTALL.bat
```

**Что делает установщик:**
1. ✅ Создаёт виртуальное окружение (venv)
2. ✅ Устанавливает все зависимости
3. ✅ Инициализирует базу данных
4. ✅ Копирует конфигурационные файлы

### Шаг 3: Настройка
Заполните конфигурационные файлы:

**telegram_config.json:**
```json
{
  "token": "8755572729:AAGAvyRp6Uni_wCbwjGIe2WRLOPUSlQ3iAc",
  "chat_ids": [701768868],
  "admin_user_id": 701768868
}
```

**vk_config.json:**
```json
{
  "token": "VK_SERVICE_TOKEN",
  "group_id": 199112265,
  "chat_peer_id": 2000000001
}
```

### Шаг 4: Запуск
```bash
# Вариант 1: Через скрипт
Start_GrafikRaboty.bat

# Вариант 2: Вручную
call venv\Scripts\activate.bat
python web_server.py
```

### Шаг 5: Проверка
Откройте в браузере:
```
http://localhost:8080
```

**Учётные данные по умолчанию:**
- **Логин:** admin
- **Пароль:** admin

---

## 🚀 ВАРИАНТ 2: ЗАПУСК EXE

### Шаг 1: Копирование
```bash
# Скопируйте папку на целевой компьютер
xcopy /E /I GrafikRaboty_Server C:\GrafikRaboty\
```

### Шаг 2: Настройка
Заполните конфигурационные файлы (см. выше)

### Шаг 3: Запуск
```bash
# Вариант 1: Через лаунчер
RUN.bat

# Вариант 2: Напрямую
GrafikRaboty_Server.exe
```

### Шаг 4: Проверка
```
http://localhost:8080
```

---

## 🤖 MCP СИСТЕМА

### Запуск MCP сервера
```bash
# Из установочного пакета
Start_MCP_Server.bat

# Или вручную
cd .mcp
python mcp_server.py
```

### Проверка MCP
```bash
curl http://localhost:8081/api/health
curl http://localhost:8081/api/status
```

### MCP Агенты (9 штук)
1. **code_fixer** — Исправление кода
2. **server_controller** — Мониторинг сервера
3. **api_tester** — Тестирование API
4. **ui_verifier** — Проверка UI
5. **security_auditor** — Аудит безопасности
6. **desktop_automation** — Автоматизация
7. **code_analyzer** — Анализ кода
8. **vk_sync_monitor** — Синхронизация VK
9. **integration_tester** — Интеграционное тестирование

---

## 📊 БАЗЫ ДАННЫХ

### schedule.db (12 таблиц)

| Таблица | Назначение |
|---------|------------|
| users | Пользователи |
| tasks | Задачи |
| work_schedule | График работ (план) |
| work_sessions | Фактические смены |
| work_journal_entries | Записи журнала |
| chat_messages | Сообщения чата |
| chat_topics | Темы чата |
| files | Файлы |
| audit_log | Аудит |
| barcodes | Штрих-коды |
| colleague_tasks | Задачи коллег |
| salary_adjustments | Корректировки зарплаты |

### Проверка БД
```bash
python check_db.py
```

### Восстановление БД
```bash
python fix_db_integrity.py
```

---

## 🔧 МОДУЛИ

### 1. Календарь
- График работ
- Задачи по дням
- Цветовая индикация

### 2. Задачи
- CRUD задачи
- Задачи коллег
- Благодарности

### 3. Файлы
- Файловое хранилище
- Предпросмотр
- Скачивание

### 4. Чат
- Корпоративный мессенджер
- Темы
- VK синхронизация

### 5. Рабочий журнал
- Открытие/закрытие смен
- Записи в течение дня
- Статистика

### 6. Конвертер ценников
- Загрузка Excel
- Настройка внешнего вида
- Печать

### 7. Штрих-коды
- Генерация
- Печать
- Импорт из Excel

### 8. Админ-панель
- Управление пользователями
- Настройки
- Аудит
- Telegram/VK интеграция

---

## 🌐 СЕТЕВЫЕ НАСТРОЙКИ

### Порты
| Порт | Назначение |
|------|------------|
| 8080 | Веб-сервер |
| 8081 | MCP сервер |

### Доступ из сети
Для доступа с других устройств:

1. Откройте порты в брандмауэре:
```bash
netsh advfirewall firewall add rule name="GrafikRaboty" dir=in action=allow protocol=TCP localport=8080
netsh advfirewall firewall add rule name="GrafikRaboty MCP" dir=in action=allow protocol=TCP localport=8081
```

2. Запустите сервер на 0.0.0.0:
```bash
python web_server.py --host 0.0.0.0
```

3. Доступ с других устройств:
```
http://<IP_адрес_сервера>:8080
```

---

## 🔐 БЕЗОПАСНОСТЬ

### Смена паролей
1. Войдите как admin
2. Перейдите в Админ-панель
3. Измените пароли пользователей

### HTTPS (опционально)
Для продакшена настройте HTTPS через reverse proxy (nginx):

```nginx
server {
    listen 443 ssl;
    server_name grafikraboty.local;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 📝 ЛОГИРОВАНИЕ

### Логи
| Файл | Назначение |
|------|------------|
| logs/web_server.log | Веб-сервер |
| logs/mcp.log | MCP система |
| logs/tunnel.log | Туннель |
| logs/vk_bot.log | VK бот |

### Просмотр логов
```bash
# Windows
type logs\web_server.log

# PowerShell
Get-Content logs\web_server.log -Tail 50
```

---

## ❓ УСТРАНЕНИЕ НЕИСПРАВНОСТЕЙ

### Порт 8080 занят
```bash
# Найти процесс
netstat -ano | findstr :8080

# Убить процесс
taskkill /F /PID <PID>

# Или используйте скрипт
Освободить_порт_8080.bat
```

### Ошибка БД
```bash
# Проверка
python check_db.py

# Восстановление
python fix_db_integrity.py
```

### Ошибка импорта
```bash
# Переустановите зависимости
pip install -r requirements.txt --force-reinstall
```

### Сервер не запускается
1. Проверьте логи: `logs/web_server.log`
2. Проверьте порты: `netstat -ano | findstr :8080`
3. Проверьте Python: `python --version`
4. Проверьте зависимости: `pip list`

---

## 🔄 ОБНОВЛЕНИЕ

### Из ZIP версии
```bash
# Остановите сервер
# Распакуйте новую версию поверх старой
# Запустите INSTALL.bat
INSTALL.bat

# Запустите сервер
Start_GrafikRaboty.bat
```

### Из EXE версии
```bash
# Остановите сервер
# Скопируйте новую папку
# Запустите RUN.bat
```

---

## 📞 ПОДДЕРЖКА

### Документация
- `README.md` — основная документация
- `ИНСТРУКЦИЯ_ПО_МИГРАЦИИ_v4.9.md` — миграция
- `ОТЧЁТ_О_ЗАВЕРШЕНИИ_МИГРАЦИИ_v4.9.md` — отчёт

### Логи
Проверьте `logs/` для диагностики

### Конфигурация
Проверьте `telegram_config.json` и `vk_config.json`

---

## 📊 ХАРАКТЕРИСТИКИ

### Системные требования
- **ОС:** Windows 10/11
- **Python:** 3.11+ (для ZIP версии)
- **Память:** 512 MB RAM
- **Место:** 500 MB
- **Порты:** 8080, 8081

### Производительность
- **Пользователи:** до 50 одновременных
- **Задачи:** до 1000 в месяц
- **Файлы:** до 10 GB
- **Сообщения:** без ограничений

---

## ✅ ЧЕКЛИСТ РАЗВЁРТЫВАНИЯ

- [ ] Распакован архив
- [ ] Запущен INSTALL.bat (ZIP версия)
- [ ] Заполнен telegram_config.json
- [ ] Заполнен vk_config.json
- [ ] Запущен сервер
- [ ] Открыт http://localhost:8080
- [ ] Выполнен вход (admin/admin)
- [ ] Изменены пароли по умолчанию
- [ ] Запущен MCP сервер (опционально)
- [ ] Настроена интеграция с Telegram
- [ ] Настроена интеграция с VK

---

**Версия:** v4.9  
**Дата:** 23.03.2026  
**Статус:** ✅ ГОТОВО К РАЗВЁРТЫВАНИЮ

*Сосямба Assistant*
