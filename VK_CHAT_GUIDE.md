# 💬 VK ЧАТ — РУКОВОДСТВО ПОЛЬЗОВАТЕЛЯ

**Версия:** 1.0  
**Дата:** 22 марта 2026 г.

---

## 🎯 ВОЗМОЖНОСТИ

### 1. **ОТПРАВКА СООБЩЕНИЙ**
- Текстовые сообщения в чат группы VK
- Мгновенная доставка через Socket.IO
- Отображение статуса отправки

### 2. **ЗАГРУЗКА ФАЙЛОВ**
- 📎 Фотографии (JPG, PNG, GIF, WEBP)
- 📄 Документы (PDF, DOC, DOCX, XLS, XLSX)
- Автоматическое сохранение на сервер
- Отправка в VK чат

### 3. **ПОЛУЧЕНИЕ СООБЩЕНИЙ**
- Входящие сообщения от сотрудников через VK
- Автоматическое сохранение в БД
- Уведомления о новых сообщениях
- Отображение вложений (фото/документы)

### 4. **ИНТЕГРАЦИЯ С VK**
- Long Poll для мгновенных уведомлений
- Привязка VK ID к пользователям системы
- Отправка в чат группы через бота

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
GrafikRaboty/
├── web_chat.py              # Модуль чата
├── templates/chat.html      # Веб-интерфейс чата
├── uploads/vk/              # Файлы из VK
│   ├── 2026-03/
│   │   ├── photo_123456.jpg
│   │   └── document_789.pdf
│   └── 2026-04/
│       └── ...
├── vk_bot.py                # VK бот (обновлён)
└── schedule.db              # БД (таблицы: vk_messages, vk_attachments)
```

---

## 🚀 ЗАПУСК

### 1. **ОТКРЫТЬ ЧАТ**

После входа в систему:
```
http://127.0.0.1:8080/chat
```

Или добавить ссылку в меню dashboard.

### 2. **ОТПРАВИТЬ СООБЩЕНИЕ**

1. Введите текст в поле ввода
2. Нажмите **➤** или **Enter**
3. Сообщение появится в чате и будет отправлено в VK

### 3. **ПРИКРЕПИТЬ ФАЙЛ**

1. Нажмите **📎**
2. Выберите файл на компьютере
3. Появится предпросмотр
4. Введите текст (опционально)
5. Нажмите **➤**

### 4. **ПОЛУЧЕНИЕ СООБЩЕНИЙ**

Когда сотрудник пишет в чат группы VK:
```
VK → Long Poll → vk_bot.py → web_chat.py → БД → Socket.IO → Чат
```

Сообщение появляется в чате мгновенно!

---

## 🔧 НАСТРОЙКА

### 1. **VK_CONFIG.JSON**

Должен содержать:
```json
{
  "service_token": "vk1.a.xxx...",
  "group_id": 199112265,
  "admin_vk_id": 146411666,
  "chat_peer_id": 2000000001,  // ID чата группы!
  "vk_user_map": {
    "146411666": 1
  },
  "api_version": "5.131"
}
```

**Важно:** `chat_peer_id` — это ID чата группы (2000000000 + group_id)

### 2. **ПРИВЯЗКА ПОЛЬЗОВАТЕЛЕЙ**

Для привязки VK ID к пользователю:
```json
"vk_user_map": {
  "146411666": 1,    // admin
  "123456789": 2,    // валерия
  "987654321": 3     // ольга
}
```

### 3. **ПРИМИНИТЬ МИГРАЦИЮ БД**

Автоматически применяется при запуске.

Таблицы:
- `vk_messages` — сообщения
- `vk_attachments` — вложения

---

## 📊 API

### GET /api/chat/messages
Получить сообщения:
```
GET /api/chat/messages?topic_id=1&limit=50
```

### POST /api/chat/send
Отправить сообщение:
```json
POST /api/chat/send
{
  "text": "Привет!",
  "topic_id": 1
}
```

### POST /api/chat/upload
Загрузить файл:
```
POST /api/chat/upload (multipart/form-data)
file: <файл>
text: "Описание"
```

### GET /api/chat/vk-status
Статус подключения VK:
```json
{
  "vk_token": "configured",
  "group_id": 199112265,
  "chat_peer_id": 2000000001,
  "status": "ok"
}
```

---

## 🔔 УВЕДОМЛЕНИЯ

### Socket.IO События

**chat_message** — новое сообщение:
```javascript
socket.on('chat_message', (data) => {
  // data: {id, user_id, full_name, message_text, ...}
});
```

**chat_connected** — подключение к чату:
```javascript
socket.on('chat_connected', (data) => {
  // {status: 'ok'}
});
```

---

## 📂 СОХРАНЕНИЕ ФАЙЛОВ

### Путь:
```
uploads/vk/YYYY-MM/<filename>
```

### Примеры:
```
uploads/vk/2026-03/photo_146411666_456789012.jpg
uploads/vk/2026-03/document_1234567890.pdf
uploads/vk/2026-04/photo_987654321_123456789.png
```

### В БД:
```sql
SELECT * FROM vk_messages WHERE has_attachment = 1;
SELECT * FROM vk_attachments WHERE message_id = 1;
```

---

## 🛠️ ОТЛАДКА

### Логи:
```
[VK VERIFY] ...
[Chat] Message sent to VK chat: ...
[Chat] File uploaded: photo.jpg
[Chat] VK message saved: id=123
```

### Проверка подключения:
```
GET /api/chat/vk-status
```

Если `status: "error"`:
1. Проверьте `service_token` в `vk_config.json`
2. Проверьте `chat_peer_id`
3. Перезапустите сервер

---

## 🎯 СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

### 1. **ОТПРАВКА ОБЪЯВЛЕНИЯ**
```
Админ → Чат → "Коллеги, завтра собрание в 10:00"
→ Все сотрудники увидят в VK
```

### 2. **ЗАГРУЗКА ДОКУМЕНТА**
```
Админ → Чат → 📎 Прикрепить → plan.pdf
→ Файл сохранится на сервере и в VK
```

### 3. **ПОЛУЧЕНИЕ ОТЧЁТА**
```
Сотрудник → VK → Бот → Фото отчёта
→ Сообщение появится в чате
→ Файл сохранится в uploads/vk/
```

---

## ✅ ГОТОВО!

**Чат работает!**

- ✅ Отправка сообщений
- ✅ Загрузка фото/документов
- ✅ Получение из VK
- ✅ Сохранение на сервер
- ✅ Мгновенные уведомления

**Сосямба реализовал! 🎉**
